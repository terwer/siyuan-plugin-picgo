## 1. Shell 现状梳理

- [x] 1.1 定位当前 PicGo 主界面打开入口，包括 topbar、menu、dialog、iframe 或 app mount 相关代码路径。
- [x] 1.2 记录当前 settings app 加载、关闭、销毁和重复打开行为。
- [x] 1.3 明确当前 SiYuan dialog 入口中哪些能力必须保留，哪些只作为兼容 fallback。

## 2. 挂载式 shell 实现

- [x] 2.1 在 `picgo-plugin-bootstrap` 中实现单例 shell 容器创建、显示、隐藏、聚焦和卸载逻辑。
- [x] 2.2 将主入口点击行为切换为打开挂载式 popup，而不是默认打开 SiYuan dialog。
- [x] 2.3 确保重复点击入口复用同一个 shell，不创建重复 DOM 容器或重复事件监听。
- [x] 2.4 确保插件卸载、禁用或 shell 关闭时清理 DOM、事件监听和运行时状态。
- [x] 2.5 保留旧 dialog 打开路径作为临时 fallback 或调试入口，并在文档中标明用途。

## 3. 迁移和初始化幂等

- [x] 3.1 追踪当前配置迁移和初始化检查的触发链路，确认为什么打开主界面会重复触发迁移。
- [x] 3.2 为迁移状态增加明确状态机或等价 guard：not-started、running、done、failed。
- [x] 3.3 将迁移触发从普通 UI open 生命周期中移出，避免已完成迁移重复运行。
- [x] 3.4 确保并发打开主界面时不会启动多个迁移任务。
- [x] 3.5 为迁移失败提供明确重试入口，不在每次打开主界面时无限自动重试。

## 4. 配置变化后的刷新提示

- [x] 4.1 盘点哪些 PicGo 或插件配置会影响菜单、命令、状态栏或运行时注册状态。
- [x] 4.2 在设置保存流程中标记 reload-required 状态，并区分可即时生效配置和需要重载配置。
- [x] 4.3 在设置界面显示清晰文案，提示菜单或运行时状态需要重载插件后生效。
- [x] 4.4 不接入自动刷新；当前实现提供手动重载/重启后的提示清除入口。
- [x] 4.5 显示手动重载插件或重启 SiYuan 的操作步骤。

## 5. 回归验证

- [ ] 5.1 验证 shell 打开、关闭、重复打开、插件卸载清理均正常。（SiYuan test 工作空间人工验证）
- [ ] 5.2 验证打开主界面多次不会重复执行已完成的配置迁移。（SiYuan test 工作空间人工验证）
- [ ] 5.3 验证配置保存后 reload-required 提示出现和清除逻辑符合预期。（SiYuan test 工作空间人工验证）
- [ ] 5.4 验证 PicGo 设置页仍能打开并保存当前图床配置。（SiYuan test 工作空间人工验证）
- [ ] 5.5 验证通过既有插件产品界面上传图片成功，Markdown 图片链接替换正常。（SiYuan test 工作空间人工验证）
- [x] 5.6 成功构建 `picgo-plugin-app` 和 `picgo-plugin-bootstrap`。
- [ ] 5.7 在 SiYuan `test` 工作空间完成插件 shell UX 冒烟验证。
